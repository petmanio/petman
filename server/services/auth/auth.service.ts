import * as config from '../../config';
import { proxyClient } from '../util/util.service';
import { IProxyOptions } from '../../types';
import { RequestPromise } from 'request-promise';
import { resolve } from 'url';

const loginService = (body): any => {

};

export { loginService };
